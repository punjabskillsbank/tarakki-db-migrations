<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="TK_112_CREATE_AUDIT_LOGS_TABLE" author="Ishmeet Singh">

        <!-- Create Audit Event Type Enum -->
        <sql>
            CREATE TYPE audit_event_type_enum AS ENUM (
                'CREATE',
                'UPDATE',
                'DELETE',
                'VIEW',
                'LOGIN',
                'LOGOUT',
                'REGISTER',
                'INVITE',
                'ASSIGN',
                'UNASSIGN',
                'STATUS_CHANGE',
                'ROLE_CHANGE',
                'PERMISSION_CHANGE',
                'MOVE',
                'ARCHIVE',
                'RESTORE',
                'EXPORT',
                'IMPORT'
            );
        </sql>

        <!-- Audit Logs Table -->
        <createTable tableName="audit_logs">

            <column name="audit_log_id" type="BIGSERIAL">
                <constraints primaryKey="true"
                             primaryKeyName="pk_audit_logs"
                             nullable="false"/>
            </column>

            <column name="request_id" type="UUID"/>

            <column name="service_name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="entity_type" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="entity_id" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="event_type" type="audit_event_type_enum">
                <constraints nullable="false"/>
            </column>

            <column name="performed_by" type="UUID">
                <constraints nullable="false"/>
            </column>

            <column name="old_value" type="JSONB"/>

            <column name="new_value" type="JSONB"/>

            <column name="ip_address" type="VARCHAR(45)"/>

            <column name="user_agent" type="TEXT"/>

            <column name="created_at" type="TIMESTAMP"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <!-- ================= Rollback ================= -->

        <rollback>

            <dropTable tableName="audit_logs"/>

            <sql>
                DROP TYPE IF EXISTS audit_event_type_enum;
            </sql>

        </rollback>

    </changeSet>

</databaseChangeLog>